#include <iostream>

#include <random>
#include <algorithm>
#include <string>
#include "Utility.hpp"
#include "Car.hpp"


int main() {
    auto cars = randomCars();
        std::cout << "Cars: " << cars << "\n";

//         TODO: print the average price and price range
    
    auto avgPrice=std::accumulate(cars.begin(), cars.end(), [](const Car& first,const Car& second){
        return (first.price() + second.price());
    });
    //couldnt get accumulate to work
        std::cout << "The average price is: $" << avgPrice << "\n";
    double priceRange = std::max(cars.begin(), cars.end())-std::min(cars.begin(), cars.end());
        std::cout << "The range is: $" << priceRange << "\n";
}
